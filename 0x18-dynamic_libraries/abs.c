#include "main.h"
/**
 * _abs - This is a fumction that displays the absolute value of integer
 * @n: Argument of int
 * Return: The absolute value of int
 */

int _abs(int n)
{
	return (n >= 0 ? n : -n);
}
