#include "main.h"

/**
 * _islower - A function tha checks for lowercase character.
 * @c: The character to be checked.
 *
 * Return: 1 if it's lowercase, 0 if uppercase.
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
