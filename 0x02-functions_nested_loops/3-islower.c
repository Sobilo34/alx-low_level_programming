#include "main.h"

/**
 * - lslower - Checks for lowercase alphabets
 *  @c : Character to be checked
 *  Return: if character ls lowercase, otherwise
 */

int - lslower(int c)
{
	if (c >= 'a' && c <= 'z')
		return (1);
	else
		return (0);
}
